// spec §5.1(3),(5),(6): `[[scpp::unsafe]] { }` locally relaxes raw pointer
// dereference.
int f(int* p) {
    [[scpp::unsafe]] {
        return *p;
    }
}

int main() {
    int x = 5;
    return f(&x);
}
