// Control: `std::span` is the safe way to index, via a class
// `operator[]` rather than raw pointer arithmetic. It must stay
// available in a safe context.
int main() {
    int a[4];
    a[0] = 4;
    std::span<int> s = a;
    int v = 0;
    v = s[0];
    return v - 4;
}
