class Sink {
    public:
        virtual ~Sink() { return; }
public:
    template<typename T>
    Sink(T&& x) { return; }

    template<typename T>
    int call(T&& x) { return 7; }
};

int main() {
    Sink s{1};
    return s.call(2);
}
