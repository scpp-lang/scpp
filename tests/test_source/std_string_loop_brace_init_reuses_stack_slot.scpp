import std;

int main() {
    int i = 0;
    std::size_t total = 0;
    while (i < 2000000) {
        std::string local{"literal"};
        total = total + local.length();
        i = i + 1;
    }
    print_int(static_cast<int>(total));
    return 0;
}
