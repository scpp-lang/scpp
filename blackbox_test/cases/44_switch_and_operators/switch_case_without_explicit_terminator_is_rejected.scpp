// PR #336: an implicit fallthrough from a non-empty `case` is ill-formed.
int main() {
    switch (1) {
        case 1:
            int value = 0;
            value = value + 1;
        case 2:
            return 0;
        default:
            return 1;
    }
}
