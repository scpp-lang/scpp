// spec §6: an unsigned type has no negative values, so -5 does not name
// one -- this used to wrap silently to 4294967291.
int f() {
    unsigned int x = -5;
    if (x == 0) { return 1; }
    return 0;
}
