// PR #414: the braced-case-body rule composes through further nesting --
// a block terminates its case if its own last statement does, recursively.
int main() {
    switch (1) {
        case 1: {
            {
                {
                    return 0;
                }
            }
        }
        default:
            return 1;
    }
}
