// ch11 §11.6: same rule, for the "no enclosing namespace at all" case --
// `export` and "lives in the required namespace" are two independent,
// both-mandatory gates.
export module foo;

export int helper() {
    return 5;
}

int main() {
    return 0;
}
