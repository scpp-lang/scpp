// spec §5.1(1): [[scpp::unsafe]] { } may nest inside another [[scpp::unsafe]]
// { } -- harmless, not an error.
int f(int* p) {
    [[scpp::unsafe]] {
        [[scpp::unsafe]] {
            return *p;
        }
    }
}

int main() {
    int x = 9;
    return f(&x);
}
