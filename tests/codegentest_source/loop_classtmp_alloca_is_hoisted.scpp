import std;

int take(std::string s) {
    return s.length() == 2 ? 2 : 0;
}

int main() {
    int i = 0;
    int total = 0;
    while (i < 3) {
        total = total + take(std::string("hi"));
        i = i + 1;
    }
    return total;
}
