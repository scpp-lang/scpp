class Box {
    public:
        virtual ~Box() { return; }
    int x{};
    int y = 7;
public:
    Box() { return; }
    int sum() { return this->x + this->y; }
};

int main() {
    Box box{};
    return box.sum() - 7;
}
