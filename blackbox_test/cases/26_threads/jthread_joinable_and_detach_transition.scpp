import std;
// A jthread is joinable after construction and not joinable after
// detach().
int main() {
    std::jthread t([]() {
        return;
    });
    if (!t.joinable()) {
        return 1;
    }
    t.detach();
    if (t.joinable()) {
        return 2;
    }
    return 0;
}
