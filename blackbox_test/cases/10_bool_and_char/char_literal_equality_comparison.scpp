// spec §16.3(4): `char` supports equality comparison (producing a
// `bool`), same as any other scalar.
int main() {
    char a = 'x';
    char b = 'x';
    char c = 'y';
    if (a == b) {
        if (!(a == c)) {
            return 1;
        }
    }
    return 0;
}
