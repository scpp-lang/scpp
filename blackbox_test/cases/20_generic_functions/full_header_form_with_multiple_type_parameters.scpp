// book ch10-01: The full header form also allows multiple type
// parameters (template<typename T, typename U> void f(T& a, U& b),
// matching generic types) -- something the abbreviated `auto`
// form has no direct equivalent for spelling as cleanly.
template<typename T, typename U>
T first(T a, U b) {
    return a;
}

int main() {
    return first(42, true);
}
