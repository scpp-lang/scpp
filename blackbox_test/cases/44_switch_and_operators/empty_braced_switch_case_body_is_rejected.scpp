// PR #414: an empty braced case body terminates nothing, so it is still an
// implicit fallthrough and stays rejected -- the block rule requires the
// block's own *last statement* to be a terminator, and `{ }` has none.
int main() {
    switch (1) {
        case 1: {
        }
        default:
            return 1;
    }
}
