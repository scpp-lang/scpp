// [expr.ass] defines `E1 += E2` as `E1 = E1 + E2`, so `p += 1` is the
// same [expr.add] pointer arithmetic spec §5.1(5.1) gates.
int main() {
    int a[4];
    int* p = &a[0];
    p += 1;
    return 0;
}
