// spec 8.2(1.2): if the condition is false before the first iteration, the
// loop body is skipped entirely.
int main() {
    int ran{};
    for (int i = 0; i < 0; i = i + 1) {
        ran = 1;
    }
    return ran;
}
