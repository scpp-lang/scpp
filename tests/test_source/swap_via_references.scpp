int swap_ints(int& a, int& b) {
    int tmp = a;
    a = b;
    b = tmp;
    return 0;
}
int main() {
    int x = 1;
    int y = 2;
    swap_ints(x, y);
    print_int(x);
    print_int(y);
    return 0;
}
