// PR #122 semantics: enum class values do not implicitly convert from ints;
// construction from the underlying type must be explicit.
enum class Status { Ok, Error };

int main() {
    Status s = 1;
    return 0;
}
