// ch05 §5.3: a function returning a reference with no reference
// parameter to elide the lifetime from must be rejected -- otherwise the
// returned reference could dangle (here, to a local that dies at
// function exit).
int& bad() {
    int x = 5;
    return x;
}

int main() {
    return 0;
}
