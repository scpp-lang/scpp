// docs/spec/en/05-unions-and-packed-layout.md §9.3:
// alignas only accepts positive power-of-two alignment values (except 0).
struct alignas(3) BadAlignment {
    int value;
};

int main() {
    return sizeof(BadAlignment);
}
