// ch06 scalar table: "`long` | alias for `int64_t` | deliberately fixed"
// -- a core builtin scalar type, not a backlog item. Currently rejected
// as a type name ("expected ';' but found 'x'").
long identity(long x) {
    return x;
}

int main() {
    long x = 5;
    return 0;
}
