// spec 8.2: nested classic `for` loops execute with ordinary C++ sequencing.
int main() {
    int count{};
    for (int i = 0; i < 3; i = i + 1) {
        for (int j = 0; j < 2; j = j + 1) {
            count = count + 1;
        }
    }
    return count - 6;
}
