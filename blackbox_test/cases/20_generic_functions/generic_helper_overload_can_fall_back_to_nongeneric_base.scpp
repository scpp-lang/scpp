int walk(int x) {
    return x + 1;
}

template<typename T>
T invoke(T x) {
    return walk(x);
}

template<typename T, typename U>
int walk(T x, U y) {
    return 0;
}

int main() {
    return invoke(1);
}
