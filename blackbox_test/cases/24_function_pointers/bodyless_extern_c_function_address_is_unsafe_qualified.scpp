// spec §5.2(4),(5): the address of... a bodyless extern "C"
// declaration (already gated the same way by spec §5.1), is a value
// of the unsafe-qualified pointer type. Taking `abs`'s address and
// calling through the resulting unsafe-qualified pointer needs
// `[[scpp::unsafe]] { }`, exactly like a directly-named bodyless
// extern "C" call already does (spec §5.1(5.6)).
extern "C" int abs(int x);

int main() {
    int (* [[scpp::unsafe]] up)(int) = abs;
    int result{};
    [[scpp::unsafe]] {
        result = up(-7);
    }
    return result;
}
