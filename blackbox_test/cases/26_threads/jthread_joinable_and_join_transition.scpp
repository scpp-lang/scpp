import std;
// A jthread is joinable after construction and no longer joinable after
// join().
int main() {
    std::jthread t([]() {
        return;
    });
    if (!t.joinable()) {
        return 1;
    }
    t.join();
    if (t.joinable()) {
        return 2;
    }
    return 0;
}
