import std;

// The same clause's closure limb: "or capturing it in a closure".
struct Holder {
    const int& ref;
};

int bad(const int& x [[scpp::lifetime(a)]]) {
    Holder h{x};
    auto f = [h]() { return h.ref; };
    return f();
}

int main() {
    int x = 1;
    return bad(x);
}
