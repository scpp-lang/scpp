// spec §8.1(4), §8.2(5): a parameter annotated
// `[[scpp::thread_shareable]]` rejects `T&`, because a mutable reference type
// is never thread-shareable.
int publish(int& value [[scpp::thread_shareable]]) {
    return value;
}

int main() {
    int x{7};
    return publish(x);
}
