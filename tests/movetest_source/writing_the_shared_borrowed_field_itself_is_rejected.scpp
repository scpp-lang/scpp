// The other half of the same rule: the borrow *does* hold `p.x`.
struct Point { int x; int y; };
int f() {
    Point p{};
    p.x = 1;
    p.y = 2;
    const int& r = p.x;
    p.x = 99;
    return r;
}
