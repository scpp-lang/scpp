// book ch05-01 / spec §6.1: an uninitialized struct local is guaranteed
// entirely zero-initialized, field-by-field -- not just for scalars.
struct Point {
    int x;
    int y;
};

int main() {
    Point p{};
    return p.x + p.y;
}
