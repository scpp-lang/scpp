import std;
template<typename T>
class Box {
    public:
        virtual ~Box() { return; }
public:
    T value;
    Box(T value) : value{std::move(value)} { return; }
};

int main() {
    return 0;
}
