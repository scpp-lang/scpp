// M1 sanity check: a program is source -> AST -> LLVM IR -> executable,
// and `main`'s return value becomes the process exit code ([undocumented rule; was old-book ch07]).
int main() {
    return 42;
}
