// spec §6: an array element is a scalar slot too -- writing through a
// subscript may not launder a conversion the same write to a local
// would have been rejected for.
int f() {
    int arr[2];
    unsigned int u = 1;
    arr[0] = u;
    if (arr[0] == 1) { return 1; }
    return 0;
}
