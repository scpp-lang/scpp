// Which element a runtime index names is not known statically, so the
// conservative whole-array answer has to stand.
int f(int i) {
    int arr[3];
    arr[0] = 1;
    arr[1] = 2;
    const int& r = arr[0];
    arr[i] = 99;
    return r;
}
