// ch03: `T&` (mutable borrow) parameters let a callee mutate the
// caller's variable.
void increment(int& r) {
    r = r + 1;
    return;
}

int main() {
    int x = 10;
    increment(x);
    return x;
}
