// spec §6.2(7)-(10): at any instant an object may have ... exactly one T&
// (mutable borrow), never two.
int f() {
    int x = 5;
    int& r1 = x;
    int& r2 = x;
    return r1 + r2;
}

int main() {
    return f();
}
