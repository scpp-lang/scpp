// spec §6: differing widths were not caught here either -- they escaped
// movecheck entirely and surfaced as an "invalid IR" internal error from
// LLVM's module verifier, with no source location.
int f() {
    int a = 1;
    long b = 2;
    if (a + b == 3) { return 1; }
    return 0;
}
