// ch05 §5.8: "Division/modulo by zero ... don't have a wrapped result to
// fall back on ... abort() unconditionally, in every context, whether
// inside [[scpp::unsafe]] { } or not."
int divide(int a, int b) {
    return a / b;
}

int main() {
    return divide(10, 0);
}
