import std;
template<typename T>
class Box {
    public:
        virtual ~Box() { return; }
public:
    T value;
    Box(T value) : value{std::move(value)} { return; }
};

int f() {
    Box<int> box{7};
    return box.value;
}
