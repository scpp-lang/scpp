class Helper {
public:
    consteval Helper(const char* s, int i) { return; }
};

class Outer {
public:
    consteval Outer(const char* s) {
        Helper h(s, 0);
        return;
    }
};

int main() {
    Outer o("x");
    return 0;
}
