// spec §6: the rule is about the two types, not about the shape of the
// place being written. A field is as much a scalar slot as a local is.
struct S { uint8_t u; };
int f() {
    S s{};
    int8_t a = 1;
    s.u = a;
    if (s.u == 1) { return 1; }
    return 0;
}
